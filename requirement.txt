# Core Framework
Django==5.0.6
djangorestframework==3.15.2

# Environment Variables
django-environ==0.11.2

# Database Driver for MySQL
mysqlclient==2.2.4
# Alternative if mysqlclient fails to install on Windows:
# mysql-connector-python==9.0.0

# CORS Handling
django-cors-headers==4.3.1

# API Documentation
drf-yasg==1.21.8

# Task Queue (Async processing)
celery==5.3.6

# Message Broker for Celery
kombu==5.3.4
amqp==5.2.0

# Optional - for better async task scheduling
billiard==4.2.0

# If you’re using Redis or RabbitMQ as broker (optional but common)
redis==5.0.8

# Development utilities (optional but recommended)
python-dotenv==1.0.1
